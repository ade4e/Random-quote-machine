{
  "name": "random-quote-machine",
  "version": "1.0.0",
  "description": "simple app generating random quotes",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '!node_modules/**/*, **/*.html, **/*.css, **/*.js' --directory --port 7777"
  },
  "author": "ade4e",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.7"
  }
}